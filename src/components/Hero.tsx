"use client";
import "./Hero.css";

export default function Hero() {
  return (
    <section className="hero">
      <div className="hero-content reveal">
        <h1 className="main-title">Night Wolf</h1>
        <p className="sub-title">Studio</p>
        <button className="gold-btn">Vstoupit do revíru</button>
      </div>
    </section>
  );
}